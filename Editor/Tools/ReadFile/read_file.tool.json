{
  "name": "read_file",
  "description": "Read the contents of a file in the Unity project. The path is relative to the Assets folder. Use this to understand existing code before making changes.",
  "input_schema": {
    "type": "object",
    "properties": {
      "file_path": {
        "type": "string",
        "description": "The file path relative to the Assets folder, e.g. 'Scripts/GameManager.cs'."
      }
    },
    "required": ["file_path"]
  }
}
